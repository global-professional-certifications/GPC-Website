# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# This repo is pnpm-only (see packageManager in package.json). Ignore the npm
# lockfile so a stray `npm install` can't reintroduce a dual-lockfile conflict,
# which previously caused different builds on Vercel vs local (blank page).
package-lock.json

# animation file
screenshots-carousel.txt
notes.txt

# Unused media files archive
Unused Files/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*.local
